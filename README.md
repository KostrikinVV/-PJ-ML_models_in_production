# 🚀 RabbitMQ + FastAPI Message Queue System

## 📋 Описание проекта

Асинхронная система обработки сообщений, использующая RabbitMQ в качестве брокера сообщений. Проект демонстрирует взаимодействие между HTTP-сервисом (FastAPI) и фоновыми обработчиками через очереди сообщений с поддержкой разделения по языкам (русский/английский).

**Суть проекта:** Создание масштабируемой микросервисной архитектуры, где HTTP-запросы асинхронно преобразуются в сообщения, которые затем обрабатываются независимыми потребителями.

## 🏗️ Архитектура системы

### Компоненты:

1. **Producer** (FastAPI) - HTTP-сервис, принимающий запросы и публикующий сообщения в RabbitMQ
    
2. **Consumer** - Фоновый процесс, читающий и обрабатывающий сообщения из очереди
    
3. **RabbitMQ** - Брокер сообщений, обеспечивающий надежную доставку
    
4. **Две очереди сообщений:**
    
    - `my_queue` - для сообщений на английском языке
        
    - `ru_queue` - для сообщений на русском языке
        

### Схема работы:

```
Пользователь → HTTP POST /publish → FastAPI → RabbitMQ → Consumer → Вывод в консоль
```

### Особенности:

- Автоматическое определение языка сообщения
    
- Durable очереди (сохраняются при перезапуске RabbitMQ)
    
- Подтверждение обработки сообщений (ack)
    
- Возможность масштабирования потребителей
    

## 🚀 Как запустить проект

### Предварительные требования:

- Python 3.10+
    
- RabbitMQ (локально или Docker)
    
- Git
    

### 1. Клонировать репозиторий

```
git clone https://github.com/KostrikinVV/rabbitmq-fastapi-demo.git
```
И открыть папку проекта
```
cd rabbitmq-fastapi-demo
```

### 2. Настройка RabbitMQ

#### Вариант A: Использование Docker (рекомендуется)
```
docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:management
```
Доступ к веб-интерфейсу RabbitMQ: [http://localhost:15672](http://localhost:15672/) (Логин/Пароль: guest/guest)

#### Вариант B: Установка локально

Установите RabbitMQ согласно официальной документации для вашей ОС

### 3. Настройка окружения

3.1 Создайте виртуальное окружение
```
python -m venv .venv
```
3.2 Активация виртуального окружения (Windows)
```
.venv\Scripts\activate
```

Активация виртуального окружения (Linux/Mac)
```
source .venv/bin/activate
```

3.3 Установите зависимости
```
pip install -r requirements.txt
```

### 4. Запуск компонентов

#### Терминал 1: Запуск Producer (FastAPI)
```
uvicorn producer:producer_app --reload
```
API будет доступно по адресу: [http://localhost:8000](http://localhost:8000/)  
Документация Swagger: [http://localhost:8000/docs](http://localhost:8000/docs)

#### Терминал 2: Запуск Consumer для английских сообщений
```
python consumer.py
```
#### Терминал 3: Запуск Consumer для русских сообщений
```
python consumer.py ru
```
### 5. Проверка работы

#### Способ A: Через Swagger UI

1. Откройте [http://localhost:8000/docs](http://localhost:8000/docs)
    
2. Найдите эндпоинт POST /publish
    
3. Нажмите "Try it out"
    
4. Введите JSON: `{"message": "Hello World"}`
    
5. Нажмите "Execute"
    

#### Способ B: Через curl

##### Английское сообщение
```
curl -X POST "http://localhost:8000/publish" \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello World!"}'
```
##### Русское сообщение
```
curl -X POST "http://localhost:8000/publish" \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет, мир!"}'
```
#### Способ C: Через Python
```
import requests

response = requests.post(
    "http://localhost:8000/publish",
    json={"message": "Test message"}
)
print(response.json())
```

## 📁 Описание файлов проекта
```
rabbitmq-fastapi-demo/
├── .env                    # Переменные окружения
├── requirements.txt       # Зависимости Python
├── producer.py           # FastAPI приложение (Producer)
├── consumer.py           # Consumer скрипт
└── README.md            # Документация
```
### producer.py

- FastAPI приложение с эндпоинтом `/publish`
    
- Автоматическое определение языка сообщения
    
- Подключение к RabbitMQ при старте
    
- Обработка HTTP запросов и публикация в очереди
    

### consumer.py

- Асинхронный потребитель сообщений
    
- Поддержка нескольких очередей через аргументы командной строки
    
- Автоматическое подтверждение обработки
    
- Корректная обработка завершения работы
    

## 🛠️ Технологический стек

### Бэкенд:

- **FastAPI** - современный, быстрый веб-фреймворк для построения API
    
- **aio-pika** - асинхронный клиент для RabbitMQ
    
- **Pydantic** - валидация данных и настройки
    
- **python-dotenv** - управление переменными окружения
    

### Брокер сообщений:

- **RabbitMQ** - надежный брокер сообщений с поддержкой AMQP
    
- **Docker** (опционально) - контейнеризация RabbitMQ
    

### Инструменты разработки:

- **Uvicorn** - ASGI-сервер для FastAPI
    
- **Swagger UI** - автоматическая документация API
    
- **RabbitMQ Management UI** - веб-интерфейс для мониторинга
    

## 📊 Результаты работы

### Что успешно реализовано:

В процессе выполнения этого учебного проекта я закрепил практические навыки работы с современными технологиями микросервисной архитектуры. Мне удалось создать полноценную систему асинхронной обработки сообщений, где разные компоненты общаются через брокер очередей.

Я научился создавать REST API на FastAPI с автоматической документацией, который принимает HTTP-запросы и преобразует их в сообщения для RabbitMQ. Асинхронный consumer, который я написал, корректно обрабатывает сообщения, выводит их в консоль и автоматически подтверждает получение.

Проект дал мне понимание, как в реальных системах организуется взаимодействие между сервисами через очереди сообщений, и как это помогает создавать масштабируемые приложения, где разные компоненты могут работать независимо друг от друга.
    

### Пример вывода при работе:

##### Producer запущен
<img width="2082" height="604" alt="image" src="https://github.com/user-attachments/assets/088575c6-0223-4148-802f-73082bee2b86" />

# Consumer для английской очереди через `curl` 
<img width="2085" height="600" alt="image" src="https://github.com/user-attachments/assets/b18becf2-3304-411a-bbb9-e356459c5be6" />
<img width="2093" height="605" alt="image" src="https://github.com/user-attachments/assets/965436e6-7d33-4cdd-9bde-09ad43fc07bc" />

# Consumer для русской очереди через Swagger UI
<img width="1675" height="991" alt="image" src="https://github.com/user-attachments/assets/50557d04-5884-41d3-bd49-1c376a5627c5" />

<img width="2082" height="602" alt="image" src="https://github.com/user-attachments/assets/bb6d0077-e6c0-4de1-9e84-881efe177db0" />

### Мониторинг:

1. **RabbitMQ Management UI** ([http://localhost:15672](http://localhost:15672/)):
<img width="2532" height="1537" alt="image" src="https://github.com/user-attachments/assets/a03e1a9b-1f45-48f0-a2be-0bb42d64aa41" />

        
2. **Swagger UI** ([http://localhost:8000/docs](http://localhost:8000/docs)):
<img width="1901" height="1070" alt="image" src="https://github.com/user-attachments/assets/ef49f2a8-7f4d-4f53-954f-659b09f339de" />
